# Contributing

Create a focused branch, run `npm run build` and `mvn test`, then open a pull request explaining the user-facing change and any migration impact. Keep API contracts validated and preserve keyboard-accessible UI behavior.

